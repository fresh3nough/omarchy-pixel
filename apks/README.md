# APKs are intentionally not tracked (GitHub 100MB limit).
# install-via-adb.sh downloads them on demand into this directory.
# Expected files when running the host installer:
#   termux.apk
#   termux-x11-noshared.apk
#   termux-widget.apk
#   termux-api.apk
